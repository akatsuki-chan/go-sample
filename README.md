# go-sample

```
$ git clone https://github.com/akatsuki-chan/go-sample.git
$ dep ensure

# Build docker image.
$ make prod

# Run docker image.
$ make docker-run ENV_MODE=prod

# Clean build/* and docker image.
$ make clean
```
